spring:
  batch:
    job:
      enabled: false
  data:
    mongodb:
      uri: mongodb://localhost:27017/test_db
logging:
  level:
    root: INFO
    com.edwise.batch: DEBUG
workout-config:
  fileName: "workout-data-test.csv"